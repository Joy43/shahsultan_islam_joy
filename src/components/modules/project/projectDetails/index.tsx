import React from 'react'
import { IProject } from '@/types/project.type';
const ProjectDetails: React.FC<{ product: IProject }> = ({ product }) => {
    return (
        <div>
        </div>
    );
}

export default ProjectDetails;
